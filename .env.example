# PROJECT AQUA — Environment Variables
#
# None required. The AI Advisor posts to /api/advisor/aqua on the host site,
# which holds the Anthropic key server-side. Running `npm run dev` on its own
# has no such endpoint and falls back to the bundled advisories in
# src/data/advisorFallbacks.js.
